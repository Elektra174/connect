# 💻 ГИД ПО ЛОКАЛЬНОМУ ТЕСТИРОВАНИЮ CONNECTUM PLATINUM

## 1. Подготовка окружения

Убедитесь, что у вас установлены:

- **Node.js** (версия 20 или выше)
- **Git** (для работы с репозиторием)
- **VS Code** (или другой редактор)

## 2. Запуск "Мозга" и Сервера

Откройте терминал в корне проекта.

### 2.1 Установите зависимости:
```bash
npm install
```

### 2.2 Заполните .env реальными ключами:

Создайте файл `.env` (если его нет) и добавьте:

```env
# Google AI API Keys (Rotation for Gemma 3 27B IT)
GOOGLE_API_KEYS=your_google_api_key_1,your_google_api_key_2,your_google_api_key_3

# Cerebras API Key (for Llama 3.3 70B)
CEREBRAS_API_KEY=your_cerebras_api_key

# Supabase (RAG Database)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_key

# Firebase
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token

# App Configuration
APP_ID=connectum-platinum
PORT=3000
```

### 2.3 Запустите бэкенд:
```bash
npm run start
```
Или:
```bash
node server.js
```

**Вы должны увидеть сообщение:**
```
🚀 Connectum Engine v20.2 Platinum Online
```

## 3. Заполнение "Векторного мозга" (RAG)

Чтобы ИИ начал выдавать экспертные советы в стиле Александра Волынского или Николая Линде, нужно запустить скрипт «прошивки»:

```bash
npm run seed
```

Или напрямую:
```bash
node scripts/seed_knowledge_base.js
```

**Что произойдет:** Скрипт превратит 300+ методологических модулей в математические векторы и загрузит их в Supabase.

## 4. Как увидеть проект "Глазами Telegram"

### Способ А: Браузерная имитация (Быстро)

Просто откройте в браузере:
```
http://localhost:3000
```

**Что вы увидите:**
- Весь дизайн Platinum
- 30 клиентов в выпадающем списке
- Чат с ИИ
- Голосовые ответы (если ключи AI настроены)
- Возможность загрузить фото

**Чего не будет:**
- Вибрации (Haptic Feedback)
- Системные кнопки Telegram

**Важно:** Код v20.2 имеет «режим разработчика». Если данных от Telegram нет, система использует `userId = 'dev_platinum'`.

### Способ Б: Через ngrok (Полноценно)

Это позволит вам открыть локальный проект прямо в приложении Telegram на телефоне.

#### 4.1 Установите ngrok:
```bash
npm install -g ngrok
```

#### 4.2 Запустите ngrok:
```bash
ngrok http 3000
```

ngrok выдаст временную HTTPS-ссылку, например:
```
https://a1b2-c3d4.ngrok-free.app
```

#### 4.3 Настройте Telegram Bot:

1. Зайдите в @BotFather
2. Выберите своего бота
3. Отправьте команду `/setwebapp`
4. Введите вашу ngrok-ссылку (например, `https://a1b2-c3d4.ngrok-free.app`)

#### 4.4 Запустите бота:

Откройте своего бота в Telegram и нажмите кнопку "Запустить" (Start).

**Готово!** Проект работает локально, но открыт внутри Telegram со всеми системными функциями.

## 5. Чек-лист проверки (Что тестировать)

### ✅ Голос
1. Зайдите в "Мастерскую"
2. Выберите клиента "Виктория" (c1)
3. Выберите модальность "МПТ"
4. Нажмите "Вступить в контакт"
5. Напишите: "Привет"
6. **Ожидаемый результат:** Вы должны услышать голос (Светлана или Дмитрий)

### ✅ RAG (Векторный поиск)
1. В чате нажмите кнопку "💡 Совет"
2. **Ожидаемый результат:** ИИ должен цитировать методы МПТ/КПТ из базы знаний

### ✅ Фото
1. Зайдите в "Профиль"
2. Нажмите на область "Фото профиля"
3. Загрузите любое фото
4. **Ожидаемый результат:** Фото должно отобразиться в кружочке
5. **Проверка в Firebase:** В консоли Firebase должно появиться фото в Storage

### ✅ 30 клиентов
1. Зайдите в "Мастерскую"
2. Откройте выпадающий список "Кейс клиента"
3. Пролистайте до конца
4. **Ожидаемый результат:** Последний клиент — Константин (c30)

### ✅ API эндпоинты
Проверьте в терминале:
```bash
# Проверка агрегатора
Invoke-RestMethod -Uri "http://localhost:3000/api/aggregator"

# Проверка чата (тестовый запрос)
Invoke-RestMethod -Uri "http://localhost:3000/api/chat" -Method POST -Body '{"userId":"test","message":"test","modalityId":"mpt","action":"chat","selectedClientId":"c1","role":"psychologist","history":[]}' -ContentType "application/json"
```

## 6. Отладка и проблемы

### Проблема: "White Screen" (Пустой экран)

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки в консоли
3. Убедитесь, что `npm run build` прошел успешно
4. Проверьте, что файл `dist/index.html` существует

### Проблема: Ошибка подключения к Supabase

**Решение:**
1. Проверьте `SUPABASE_URL` и `SUPABASE_KEY` в `.env`
2. Убедитесь, что таблица `knowledge_base` создана в Supabase
3. Запустите `npm run seed` для заполнения базы

### Проблема: Ошибка AI (Gemma/Llama)

**Решение:**
1. Проверьте `GOOGLE_API_KEYS` и `CEREBRAS_API_KEY` в `.env`
2. Убедитесь, что ключи активны и не превысили лимит
3. Проверьте логи сервера в терминале

### Проблема: Firebase ошибка

**Решение:**
1. Проверьте `FIREBASE_SERVICE_ACCOUNT` в `.env`
2. Убедитесь, что проект Firebase создан
3. Проверьте права доступа в Firebase Console

## 7. Полезные команды

```bash
# Запуск сервера
npm run start

# Запуск в режиме разработки (с перезагрузкой)
npm run dev

# Сборка фронтенда
npm run build

# Заполнение RAG базы
npm run seed

# Проверка статуса сервера
netstat -ano | findstr :3000

# Остановка процесса на порту 3000
taskkill /PID <PID> /F
```

## 8. Деплой на Render (Production)

1. Создайте новый Web Service на Render
2. Подключите GitHub репозиторий
3. Установите команду запуска: `npm run start`
4. Добавьте переменные окружения (все ключи из `.env`)
5. Деплой запустится автоматически

**URL будет:** `https://your-project.onrender.com`

## 9. Важные заметки

- **Не коммитьте `.env` файл** — он содержит секретные ключи
- **Используйте `.env.example`** как шаблон для настройки
- **RAG база должна быть заполнена** перед использованием кнопки "Совет"
- **Telegram WebApp требует HTTPS** — используйте ngrok для локального тестирования
- **Все 30 клиентов в одном файле** — никакого дробления на компоненты

## 10. Контакты и поддержка

Если возникли проблемы:

1. Проверьте логи сервера в терминале
2. Откройте консоль браузера (F12)
3. Проверьте документацию в `/docs`
4. Обратитесь к администратору

---

**Connectum Pro Platinum v20.2** — готов к использованию! 🚀
