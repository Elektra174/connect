📜 КОНСТИТУЦИЯ РАЗРАБОТКИ CONNECTUM PRO PLATINUM

Этот документ является обязательным для ознакомления ИИ-агентом (Cline/Cursor/Windsurf) перед началом любых работ. Нарушение этих правил ведет к разрушению архитектуры проекта.

1. ПРАВИЛО №1: ANTI-CUTTING (ЗАПРЕТ НА СОКРАЩЕНИЯ)

Никогда не сокращай код. Если ты обновляешь файл (особенно App.jsx, server.js или prompt_manager.js), ты обязан выдавать его ЦЕЛИКОМ.

Запрещено использовать комментарии вида // ... остальной код без изменений или // ... (30 клиентов здесь).

Любая выдача кода — это Full Version Monolith. Если файл велик, пиши его полностью. Это критично для сохранения целостности 30 кейсов клиентов и сложных CSS-стилей.

2. ПРАВИЛО №2: АРХИТЕКТУРНАЯ ЦЕЛОСТНОСТЬ

Frontend: React Monolith (App.jsx). Все компоненты, хуки и стили живут в одном файле. Это необходимо для корректного деплоя в Telegram Mini App.

Backend: Node.js Express (server.js). Все API-эндпоинты, AI-оркестрация и TTS-логика находятся здесь.

Database (Rule #1): * Публичные данные: /artifacts/{appId}/public/data/{collectionName}

Приватные данные: /artifacts/{appId}/users/{userId}/{collectionName}

Нарушение путей ведет к ошибке Firebase Permissions.

3. ПРАВИЛО №3: ЛИНГВИСТИЧЕСКИЙ СТАНДАРТ

Язык интерфейса: Только русский.

Язык ответов ИИ: Только русский.

Gender Lock: ИИ-клиент обязан соблюдать род речи выбранного персонажа (я пришел / я пришла).

Буква «Ё»: Запрещено принудительное использование «ё» (используй «е»), если только это не часть цитаты из базы знаний.

Латиница: Категорический запрет на английские слова в сообщениях пользователю.

4. ПРАВИЛО №4: ДИЗАЙН И ЭСТЕТИКА (PLATINUM UI)

Сохраняй цветовую схему: #020617 (фон), Indigo/Purple (акценты), Emerald (ресурсные состояния).

Не удаляй mesh-bg и outlined-text (магнитные кнопки).

Все иконки должны быть реализованы как чистые SVG-компоненты внутри App.jsx, чтобы избежать ошибок рендеринга и конфликтов имен (например, с Infinity).

5. ПРАВИЛО №5: БИЗНЕС-ЛОГИКА И ТАРИФЫ

Тарифы для Психологов: Тест-драйв (490₽), PRO (2990₽).

Тарифы для Клиентов: Premium (1990₽).

Комиссия: PRO — 20%, Basic — 40%.

Любое изменение в логике оплат должно логироваться и отправляться админу (ID 7830322013).

6. ПРАВИЛО №6: ТЕХНОЛОГИЧЕСКИЙ СТЕК И ЗАВИСИМОСТИ

Запрещено самовольно менять библиотеки, указанные в package.json.

TTS Engine: Используется строго msedge-tts. Это существующий и валидный npm-пакет. Не пытайся заменить его на microsoft-cognitiveservices-speech-sdk, так как это потребует платных ключей Azure.

Если ты считаешь, что пакета не существует — проверь npm info msedge-tts или загляни в текущий package.json.

🛠 ТЕХНИЧЕСКИЙ ЧЕК-ЛИСТ ПЕРЕД ОПЕРАЦИЕЙ «SAVE»:

Проверь, на месте ли все 30 клиентов в массиве CLIENT_DATABASE.

Проверь, не удалены ли методы 6 школ терапии из prompt_manager.js.

Убедись, что иконка Infinity не конфликтует с глобальным объектом JS.

Убедись, что пути Firestore соответствуют Rule #1.

ПРИНЯТИЕ ИНСТРУКЦИИ:
Агент обязан подтвердить понимание этих правил фразой: "Я ознакомлен с протоколом Platinum v20.2 и обязуюсь сохранять целостность монолита."