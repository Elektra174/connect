/**
 * CONNECTUM PRO v21.25 - KNOWLEDGE SEEDER (YANDEX EDITION)
 * ========================================================
 * 🚀 ЗАДАЧА: Первичная прошивка или обновление базы знаний.
 * 🧠 МОДЕЛЬ: Yandex text-search-query (оптимизировано для RU).
 * 📂 DATABASE: Supabase (pgvector) - хранение смысловых векторов.
 * 🛡️ LOGIC: Пакетная загрузка с защитой от перегрузки API.
 */

require('dotenv').config();
const { createClient } = require('@supabase/supabase-js');
const axios = require('axios');
const winston = require('winston');

// --- 📝 ЛОГИРОВАНИЕ ---
const logger = winston.createLogger({
    level: 'info',
    format: winston.format.combine(winston.format.timestamp(), winston.format.json()),
    transports: [
        new winston.transports.File({ filename: 'logs/seed_yandex.log' }),
        new winston.transports.Console({
            format: winston.format.combine(winston.format.colorize(), winston.format.simple())
        })
    ],
});

// --- ⚙️ КОНФИГУРАЦИЯ ---
const YANDEX_API_KEY = process.env.YANDEX_API_KEY;
const FOLDER_ID = process.env.YANDEX_FOLDER_ID;
const SUPABASE_URL = process.env.SUPABASE_URL;
const SUPABASE_KEY = process.env.SUPABASE_KEY;

if (!YANDEX_API_KEY || !FOLDER_ID || !SUPABASE_URL || !SUPABASE_KEY) {
    console.error("❌ Ошибка: Проверьте переменные окружения (YANDEX_API_KEY, FOLDER_ID, SUPABASE_URL, SUPABASE_KEY)");
    process.exit(1);
}

const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);

/**
 * 📚 РЕЕСТР ЭТАЛОННЫХ ЗНАНИЙ (Ядро Connectum)
 * Включает ключевые интервенции по всем модальностям.
 */
const KNOWLEDGE_DATA = [
    // --- 💎 МПТ (АЛЕКСАНДР ВОЛЫНСКИЙ) ---
    { mod: 'mpt', cat: 'Авторство', trigger: 'Клиент говорит "оно само", "меня мучает"', text: 'Что ТЫ делаешь прямо сейчас, чтобы это продолжалось? Покажи телом это действие.', marker: 'Переход в субъектную позицию' },
    { mod: 'mpt', cat: 'Целостность', trigger: 'Страх внешней силы или авторитета', text: 'Представь, что ты и есть эта сила. Какое качество бытия ты чувствуешь из этой роли? Проблема остается?', marker: 'Обнаружение ресурса через идентификацию' },
    { mod: 'mpt', cat: 'Точка решения', trigger: 'Клиент застрял в описании боли', text: 'Представь, что проблема решена. Опиши свое состояние из этой точки будущего. Как дышится?', marker: 'Телесное расширение, вдох' },
    { mod: 'mpt', cat: 'Позитивная цель', trigger: 'Саботаж, лень, откладывание', text: 'От какой опасности тебя бережет этот саботаж? Поблагодари его за защиту. Что изменится?', marker: 'Снятие вины, легализация отдыха' },
    { mod: 'mpt', cat: 'Идентичность', trigger: 'Я никчемный / Я дефектный', text: 'Кто это говорит? Стань этим критиком. Какую свою силу ты вкладываешь в это подавление?', marker: 'Присвоение агрессии' },

    // --- 🧩 КПТ (БЕК) ---
    { mod: 'cbt', cat: 'Диспут', trigger: 'Катастрофизация (Все пропало)', text: 'Какова реальная вероятность худшего исхода в цифрах? Какие есть факты против этой мысли?', marker: 'Логическая опора' },
    { mod: 'cbt', cat: 'Долженствование', trigger: 'Я должен быть идеальным', text: 'Что произойдет, если заменить "должен" на "хотел бы"? Становишься ли ты от этого хуже как человек?', marker: 'Снятие зажима в груди' },
    { mod: 'cbt', cat: 'Эксперимент', trigger: 'Страх оценки окружающих', text: 'Давай проведем эксперимент: соверши маленькую ошибку специально. Что ты заметишь в реакции людей?', marker: 'Готовность к новому действию' },

    // --- 🌊 ГЕШТАЛЬТ (ПЕРЛЗ) ---
    { mod: 'gestalt', cat: 'Контакт', trigger: 'Рационализация (Уход в голову)', text: 'Я вижу, как ты сейчас задержал дыхание. Что ты чувствуешь ко мне в этот момент?', marker: 'Возврат в "Здесь и Сейчас"' },
    { mod: 'gestalt', cat: 'Полярности', trigger: 'Внутренний конфликт частей', text: 'Посади свою "ленивую часть" на пустой стул. О чем она хочет попросить твою "деятельную часть"?', marker: 'Интеграция конфликта' },

    // --- 🎨 ЭОТ (ЛИНДЕ) ---
    { mod: 'eit', cat: 'Трансформация', trigger: 'Тяжесть как образ камня/узла', text: 'Стань этим камнем. Чего ты хочешь для человека, в котором находишься? Дай образу ресурс.', marker: 'Изменение цвета/веса образа' },
    { mod: 'eit', cat: 'Инвестиции', trigger: 'Дыра в сердце (потеря любви)', text: 'Верни себе ту любовь, которую ты отдал этому человеку. Вдохни её как золотой свет обратно.', marker: 'Ощущение наполненности' },

    // --- 🎭 ТА (БЕРН) ---
    { mod: 'ta', cat: 'Эго-состояния', trigger: 'Жалоба из позиции Жертвы', text: 'Похоже, сейчас говорит твой Адаптивный Ребенок. Что по этому поводу думает твой Взрослый?', marker: 'Смена интонации на ровную' },
    { mod: 'ta', cat: 'Игры', trigger: 'Игра "Да, но..." (Обесценивание)', text: 'Какой твой скрытый выигрыш в том, чтобы не решать проблему прямо сейчас?', marker: 'Инсайт о поведении' },

    // --- 🕰 ПСИХОАНАЛИЗ (ФРЕЙД) ---
    { mod: 'psychoanalysis', cat: 'Перенос', trigger: 'Ожидание оценки терапевта', text: 'На кого я сейчас похож, когда ты ждешь моего одобрения? Какие чувства это вызывает?', marker: 'Связь с детским опытом' },
    { mod: 'psychoanalysis', cat: 'Ассоциации', trigger: 'Тупик в рассказе', text: 'Не думай. Какие три случайных слова приходят на ум в связи с этим образом из сна?', marker: 'Доступ к бессознательному' }
];

/**
 * ГЕНЕРАЦИЯ ЭМБЕДДИНГА ЧЕРЕЗ YANDEX CLOUD
 */
async function getYandexEmbedding(text) {
    const url = 'https://llm.api.cloud.yandex.net/foundationModels/v1/textEmbedding';
    const payload = {
        modelUri: `emb://${FOLDER_ID}/text-search-query/latest`,
        text: text
    };

    try {
        const res = await axios.post(url, payload, {
            headers: {
                'Authorization': `Api-Key ${YANDEX_API_KEY}`,
                'x-folder-id': FOLDER_ID
            }
        });
        return res.data.embedding;
    } catch (e) {
        logger.error("Yandex Embedding Error: " + (e.response?.data?.message || e.message));
        return null;
    }
}

/**
 * ОСНОВНОЙ ЦИКЛ ЗАГРУЗКИ
 */
async function run() {
    console.log(`🚀 ЗАПУСК МИГРАЦИИ НА YANDEX: ${KNOWLEDGE_DATA.length} модулей.`);

    // 1. Опционально: Очистка старой базы (если нужно начать с нуля)
    // const { error: deleteError } = await supabase.from('knowledge_base').delete().neq('id', 0);
    // if (deleteError) logger.warn("Не удалось очистить старую базу.");

    for (const [index, item] of KNOWLEDGE_DATA.entries()) {
        try {
            // Формируем текст для векторизации (такой же как в server.js для совпадения контекста)
            const combinedText = `СИТУАЦИЯ: ${item.trigger} | МЕТОД: ${item.mod} | ИНТЕРВЕНЦИЯ: ${item.text}`;
           
            const embedding = await getYandexEmbedding(combinedText);

            if (!embedding) {
                console.error(`❌ Пропуск модуля [${index + 1}]: Ошибка API Яндекса`);
                continue;
            }

            const { error } = await supabase.from('knowledge_base').insert({
                modality_id: item.mod,
                category: item.cat,
                context_trigger: item.trigger,
                content: item.text,
                embedding: embedding
            });

            if (error) throw error;
           
            console.log(`✅ [${index + 1}/${KNOWLEDGE_DATA.length}] Вектор Yandex загружен: ${item.mod}`);

            // Пауза 0.5 сек для стабильности (у Яндекса лимиты выше, чем у Google Free)
            await new Promise(resolve => setTimeout(resolve, 500));
        } catch (e) {
            console.error(`❌ Сбой на модуле "${item.trigger}":`, e.message);
        }
    }

    console.log("\n🏁 ФИНАЛ: База знаний успешно переведена на 'язык' YandexGPT Pro!");
}

run();
