# Техническая спецификация архитектуры Connectum Pro (v20.2 Platinum)

## 1. FRONTEND: React Monolith (App.jsx)

Фронтенд реализован как одностраничное приложение (SPA) на React 18, упакованное в один файл для оптимальной работы внутри Telegram Mini App.

### 🧩 Ключевые компоненты:

**App (Main):** Центральный хаб состояний. Управляет навигацией (screen), текущей ролью юзера, историей чата и профилем.

**VideoRecorder:** Инструмент для записи видео-визиток. Использует MediaRecorder API, ограничивает запись 60 секундами и возвращает Blob.

**RadarChart:** SVG-компонент для визуализации компетенций психолога по 4 осям (Метод, Эмпатия, Границы, Этика).

**Knowledge Hub (в library):** Интерфейс поиска и фильтрации эталонных модулей по 6 школам терапии.

### 📱 Экраны (Navigation Logic):

1. **hub:** Главный вход (Выбор: Психолог / Клиент).
2. **client_hub:** Клиентский центр с тарифами и услугами.
3. **setup:** Мастерская. Выбор школы (МПТ, КПТ и др.) и выбор одного из 30 клиентов.
4. **chat:** Игровой интерфейс. Поддерживает режимы симуляции и получения подсказок (get_hint).
5. **aggregator:** Публичная витрина. Список мастеров с ранжированием VIP/PRO/Rating.
6. **profile:** Личный кабинет. Редактирование анкеты и запись видео.
7. **library:** Библиотека знаний. Доступ к 300+ атомам методологии.

## 2. BACKEND: Node.js Engine (server.js)

Сервер является «оркестратором», который связывает интерфейс с искусственным интеллектом и базами данных.

### 🧠 AI-Архитектура (Hybrid AI):

**Primary (Llama 3.3 70B via Cerebras):** Используется для мгновенных ответов ИИ-клиента в чате (скорость > 2000 токенов/сек).

**Analysis (Gemma 3 27B IT via Google):** Используется для глубокого аудита сессий, генерации JSON-отчетов и подсказок Супервизора.

**RAG Layer (Supabase Vector):** Перед ответом сервер делает семантический поиск по 300 модулям в Supabase, находя 3 наиболее релевантных совета.

### 📡 API Эндпоинты:

**POST /api/chat:** Основной цикл. Принимает сообщение, делает инъекцию знаний из RAG, вызывает ИИ, генерирует голос (TTS) и возвращает ответ.

**POST /api/finish:** Завершение сессии. Проводит аудит через Gemma 3, генерирует PDF-сертификат и сохраняет результат в Firestore.

**GET /api/aggregator:** Возвращает отсортированный список психологов (VIP -> PRO -> Video -> Rating).

**POST /api/upload-video:** Принимает Base64 видео, сохраняет в Firebase Storage с флагом overwrite (один файл на юзера).

**POST /api/profile:** Синхронизирует данные между приватным профилем юзера и публичной витриной.

## 3. LOGIC LAYER: Prompt Engineering (prompt_manager.js)

Файл, определяющий «личность» и «профессионализм» системы.

### Функции генерации промптов:

**generateClientPrompt:** Создает роль клиента. Включает Gender Lock (строгий род речи) и инструкции по сопротивлению/раскрытию на основе методов школы.

**generateSupervisorPrompt:** Формирует роль наставника. Требует краткости (до 12 слов) и методической точности.

**generateDeepAnalysisPrompt:** Инструкция для создания JSON-объекта аналитики. Оценивает сессию по 4 KPI.

**generateAiTherapistPrompt:** Инструкция для ИИ-терапевта (B2C режим).

**generateClientSummaryPrompt:** Инструкция для создания карты осознанности клиента.

## 4. DATA LAYER: Базы данных и Хранилище

### 📁 Firebase Firestore (NoSQL):

**Приватный путь:** `/artifacts/{appId}/users/{userId}/` — логи тренировок, личные настройки, сертификаты.

**Публичный путь:** `/artifacts/{appId}/public/data/` — анкеты для агрегатора, лист ожидания.

### 🗄 Supabase (Vector DB):

**Таблица knowledge_base.** Содержит текст техник и их Embedding (вектор 768). Используется для RAG-поиска (функция match_knowledge).

### ☁️ Firebase Storage:

**Папка videos/:** Хранение видео-визиток.

**Папка certificates/:** Хранение PDF-отчетов.

## 5. ВНЕШНИЕ ИНТЕГРАЦИИ

**Telegram Bot API:** Обработка команды /start, отправка Webhook-уведомлений админу (ID 7830322013).

**Microsoft Edge TTS:** Генерация голоса. Использует SvetlanaNeural (женский) и DmitryNeural (мужской/системный).

**Google Embedding API (text-embedding-004):** Математическое кодирование смыслов для поиска в базе знаний.

## 6. ПРОЕКТНАЯ СТРУКТУРА (Directory Tree)

```
/root — Корень проекта.
  server.js — Монолитный бэкенд (Express, Bot API, AI Orchestrator).
  prompt_manager.js — Логика формирования системных инструкций для ИИ.
  modalities.js — Методологический справочник 6 школ психотерапии.
  db_schema.js — Описание структуры и путей Firestore (Rule #1).
  package.json — Конфигурация зависимостей Node.js.
  .env — Файл секретов (API ключи, Firebase Config).
  /src — Папка исходного кода фронтенда.
    App.jsx — Главный React-монолит (Интерфейс + Логика Mini App).
    main.jsx — Точка входа для сборщика (Vite/Webpack).
  /public — Статические файлы.
    index.html — HTML-оболочка для React-приложения.
  /scripts — Служебные инструменты.
    seed_knowledge_base.js — Скрипт для загрузки 300+ модулей в Supabase RAG.
  /docs — Техническая документация.
    architecture_specification.md — Текущая спецификация (этот файл).
    rag_implementation_guide.md — Инструкция по работе с векторной базой.
```

## 7. ТЕКУЩИЙ СТАТУС И ОТЛАДКА (Implementation Log)

### ⚠️ Проблема «Белого экрана» (Blank Screen):

**Вероятные причины для монолита App.jsx v20.2:**

1. **Ошибка импорта Lucide:** Убедитесь, что lucide-react установлена (`npm i lucide-react`). В коде v19.9 используется много иконок.
2. **Missing marked:** Библиотека `marked` должна быть импортирована для рендеринга Markdown в чате.
3. **Telegram WebApp Object:** При запуске вне Telegram (в обычном браузере) `window.Telegram.WebApp` может быть undefined. В коде должна быть заглушка для userId.

### ✅ Что РЕАЛЬНО реализовано (Functional Audit):

**Бэкенд:** Полная поддержка RAG через Supabase. Эндпоинты `/api/chat` и `/api/finish` полностью функциональны.

**ИИ:** Логика разделения Llama (диалог) и Gemma (аналитика) прописана в server.js.

**База клиентов:** Массив из 30 клиентов (с1-с30) встроен в оба конца (фронт и бэк).

**Голос:** Поддержка MS Edge TTS интегрирована в server.js.

### 🛠 Ближайшее действие для Cline:

Открыть App.jsx и проверить консоль браузера (F12).
Найти конкретную JS-ошибку (вероятно, "is not defined" или "cannot read property of undefined").
Исправить рендеринг, чтобы появился экран hub.

## 8. ВАЖНЫЕ ПРИМЕЧАНИЯ

**Rule #1 (СТРОГИЕ ПУТИ):**
- Публичные: `/artifacts/{appId}/public/data/{collectionName}/{docId}`
- Приватные: `/artifacts/{appId}/users/{userId}/{collectionName}/{docId}`

**Rule #2 (СЕРВЕРНАЯ СОРТИРОВКА):**
- Ранжирование: VIP -> PRO -> Видео -> Фото -> SkillRating

**Rule #3 (AUTH):**
- Доступ к данным разрешен только после `signInAnonymously()` или `signInWithCustomToken()`.

## 9. БИЗНЕС-ЛОГИКА И ТАРИФЫ

### Тарифы для Психологов:
- **Test-Drive:** 490₽ (3 дня симуляций)
- **PRO:** 2990₽ (Месяц + Рейтинг)

### Тарифы для Клиентов:
- **Premium:** 1990₽ (ИИ-терапия 24/7 + Приоритет)

### Комиссия:
- **PRO:** 20%
- **Basic:** 40%

## 10. ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Frontend:
- React 18.2.0
- Vite 5.0.12
- Tailwind CSS 3.4.1
- Lucide React 0.312.0
- Marked 11.1.1

### Backend:
- Node.js 20.x
- Express 4.18.2
- Google Generative AI 0.21.0
- Cerebras API (Llama 3.3 70B)
- Supabase JS 2.39.3
- Firebase Admin 12.0.0
- MS Edge TTS 2.0.3
- PDFKit 0.14.0
- Node Telegram Bot API 0.66.0

### Database:
- Firebase Firestore (NoSQL)
- Supabase (Vector DB)

## 11. СООТВЕТСТВИЕ СПЕЦИФИКАЦИИ

Данный документ соответствует технической спецификации v20.2 Platinum и описывает:
- ✅ Полный стек технологий
- ✅ Все API эндпоинты
- ✅ Структуру данных
- ✅ Логику работы ИИ
- ✅ Бизнес-модель
- ✅ Правила архитектуры

**Connectum Pro v20.2 Platinum** — это завершенная, масштабируемая архитектура, объединяющая психологическую экспертизу с передовыми технологиями генеративного ИИ.
